discord:
  # Permissions applied to every Discord account
  default_permissions: []

  # Per-account permissions by Discord user ID
  # Use exact permission nodes only (e.g. solacecore.ban, solacecore.mute)
  # use solacecore.* for all plugin permissions
  accounts:
    "123456789012345678":
      - "solacecore.*"
